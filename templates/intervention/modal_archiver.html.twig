{% if is_granted('ROLE_TRAVAUX_ADMIN') or is_granted('ROLE_TRAVAUX_REDACTEUR') %}
    <!-- Modal -->
    <div class="modal fade" id="archiveModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel"
         aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                    <h4 class="modal-title"
                        id="myModalLabel">{% if intervention.archive %}Désarchiver{% else %}Archiver{% endif %} une
                        intervention</h4>
                </div>
                <div class="modal-body">
                    <br/> <strong>{{ intervention }} </strong>
                </div>
                <div class="modal-footer">
                    <a href='#' type="button" class="btn btn-default pull-left" data-dismiss="modal">Annuler</a>
                    {{ form(archive_form) }}
                </div>
            </div>
        </div>
    </div>
{% endif %}