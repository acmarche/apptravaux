{% if is_granted('ROLE_TRAVAUX_ADMIN') %}
           <div class="btn-toolbar" role="toolbar" style="margin: 0;">
                <div class="btn-group">
                    <a href="{{ path('categorie') }}" class="btn btn-primary" role="button"><i class="glyphicon glyphicon-list"></i> Retour à la liste</a>
                </div> 
                <div class="btn-group">
                    <a href="{{ path('categorie_edit', { 'slugname': entity.slugname }) }}" class="btn btn-primary" role="button"><i class="glyphicon glyphicon-edit"></i> Editer</a>
                </div>                
            </div>
    <br />
{% endif %}