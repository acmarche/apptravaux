{% if avaloir.imageName %}
    <img src="{{ vich_uploader_asset(avaloir, 'imageFile') | imagine_filter('avaloir_thumb') }}"
         alt="{{ avaloir.imageName }}"/>
    <br/>    <br/>

    <h4>Image Google SteetView</h4>    <br/>

    <div class="alert alert-info">
        Test avec le système de google :-P
    </div>

    <img src="data:image/jpeg;base64,{{ avaloir|street_view(avaloir.latitude, avaloir.longitude) }}" alt="google">

{% endif %}
<br/>
